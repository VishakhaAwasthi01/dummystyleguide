import React from "react";
import Button from "@mui/material/Button";
const RedBtnComponent = () => {
  return (
    <Button variant="outlined" color="error">
      Click Me
    </Button>
  );
};

export default RedBtnComponent;
